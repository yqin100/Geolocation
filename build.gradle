ext.jarExtension = '.jar'
ext.esriJarName = JAR_ESRI_BASENAME + '-' + VERSION_PROJECT + jarExtension
ext.coreJarName = JAR_CORE_BASENAME + '-' + VERSION_PROJECT + jarExtension
ext.businessJarName = JAR_BUSINESS_BASENAME + '-' + VERSION_PROJECT + jarExtension
ext.MY_MANIFEST_CLASSPATH = coreJarName + ' ' + businessJarName